# Pinned for reproducible deployments (deploy.sh update rebuilds the image
# from this file). To upgrade: bump versions deliberately and run the tests.

# Web UI
fastapi==0.139.0
uvicorn==0.51.0

# Exchange / HTTP
requests==2.33.1

# Analysis & LLM
tradingview-ta==3.3.0
anthropic==0.116.0
openai==2.45.0

# Scheduling
astral==3.2
pytz==2026.2

# Config
python-dotenv==1.2.2
